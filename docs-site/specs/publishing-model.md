# Publishing Model

Documento espelhado a partir de `docs/specs/publishing-model.md` no repositório principal.

Este documento cobre:

- publicacao por host
- install, sync e upgrade
- ownership de arquivos
- separacao entre managed, mergeable e custom
